apiVersion: "v1"
kind: "Service"
metadata:
  name: "grafana-1-svc"
  namespace: "default"
spec:
  ports:
  - protocol: "TCP"
    port: 80
    targetPort: 3000
  selector:
    app.kubernetes.io/name: grafana-1
  type: "NodePort"
